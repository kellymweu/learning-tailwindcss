Paste these into tailwind config

/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        chestnut: "#973F29"
      }
    },
  },
  plugins: [],
}
